import React from 'react';
import { StyleSheet, Text, TouchableOpacity } from 'react-native';
import { Ionicons } from '@expo/vector-icons';

export default function SpotTag({ locationName, distance }) {
  return (
    <TouchableOpacity style={styles.container} activeOpacity={0.8}>
      <Ionicons name="location-sharp" size={12} color="#FF2D55" style={{ marginRight: 4 }} />
      <Text style={styles.text}>{locationName} • {distance}km</Text>
    </TouchableOpacity>
  );
}

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    backgroundColor: 'rgba(0, 0, 0, 0.6)',
    paddingHorizontal: 10,
    paddingVertical: 4,
    borderRadius: 12,
    alignSelf: 'flex-start',
    marginTop: 6,
    borderWidth: 1,
    borderColor: 'rgba(255, 45, 85, 0.3)',
  },
  text: {
    color: '#FFF',
    fontSize: 11,
    fontWeight: '500',
  },
});
